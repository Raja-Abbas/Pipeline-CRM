[build]
  command = "npm install && npx prisma db push && npx prisma generate && npx next build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"
  NODE_OPTIONS = "--max-old-space-size=4096"
